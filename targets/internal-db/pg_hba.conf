# pg_hba.conf — INTENTIONALLY PERMISSIVE for security testing
# This simulates a misconfigured database that trusts all connections
# from the tenant network without requiring passwords.
#
# TYPE  DATABASE  USER    ADDRESS          METHOD
local   all       all                      trust
host    all       all     0.0.0.0/0        trust
host    all       all     ::/0             trust
